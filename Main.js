function drawMap(map) {
    /* Takes a map as a javascript array and draws the map on the screen */
    for (var row = 0; row < sampleMap.length(); i++) {

    }
}

sampleMap = [[0,0,0], [0,0,0], [0,0,0]]